import LoadingSpinner from '../../../core/components/common/LoadingSpinner'

// Stub temporal — el panel completo llega en la tarea siguiente.
export default function ValidarPorLote() {
  return <LoadingSpinner />
}
